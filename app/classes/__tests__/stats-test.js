var Stats = require('../stats');

describe('Stats', function() {
  describe('expenseByMonth', function() {
    it('returns the sum all the expenses in the given month', function() {
      expenses = Stats.expenseByMonth()
    });
  });
  //describe('currentMonthAccumulatedExpense', function() {
  //  it('returns the accumulated expense for the current month', function(){
  //    accumulatedExpense =

  //  });
  //});
  
});
